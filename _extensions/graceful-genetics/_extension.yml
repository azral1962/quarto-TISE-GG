title: Graceful Genetics
author: James R. Swift (original Typst template); Armein Z. R. Langi (Quarto adaptation)
version: "0.1.0"
quarto-required: ">=1.10.0"
contributes:
  formats:
    typst:
      template-partials:
        - typst-template.typ
        - typst-show.typ
      papersize: a4
      font-paths:
        - fonts
      fig-format: svg

